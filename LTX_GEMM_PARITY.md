# LTX GEMM — план достижения паритета (cu_perf)

> Живой трекер. Цель: LTX ~2× медленнее python; узкое = наши GEMM-ядра.
> Достичь **полного паритета** (или x-улучшения) по КАЖДОМУ пункту, бить по **худшей** точке.
> **ГЛАВНОЕ ПРАВИЛО: не потерять качество.** За качество мы и заплатили скоростью
> (NVFP4 m>1→Broadcast, mxfp8-full opt-in, bf16 float-acc — всё ради корректности).
> Любое ускорение гейтить **per-row max-abs**, НЕ глобальным cos (см. feedback_perrow_not_cos_gate).

## Методология замера (важно — чтобы воспроизводить)
- RTX 5090 **Laptop, 24GB**, sm_120a. Системная RAM 93GB, **swap=0**.
- **Бенчить СТРОГО по одному процессу** (параллель = OOM-фриз). Обёртка:
  `systemd-run --user --scope -p MemoryMax=72G -p MemorySwapMax=0 --quiet <cmd>`.
- NVRTC/JIT вырезаны прогревом (rust warmup=10; python `triton.do_bench(warmup=25)`).
- TFLOP/s = 2·M·N·K / time.
- Наш бенч: `crates/synaptix-kernels-cuda/examples/bench_ltx_gemm.rs`
  (`cargo run --profile fast-release --features cuda --example bench_ltx_gemm -p synaptix-kernels-cuda`).
- qutlass/cuBLAS: `~/Temp/cuda-kernels-zoo/qutlass/bench_ltx_gemm.py`
  (venv `synaptix/scripts/reference/.venv`, `PYTHONPATH=~/Temp/cuda-kernels-zoo/qutlass`).
- Формы LTX-2.3 DiT (video, inner=4096, ff=16384, 48 блоков): attn `(N=4096,K=4096)`,
  ff_up `(16384,4096)`, ff_down `(4096,16384)`. M = video-токены (512²→~1792, 768×512→~5k,
  720p→~12k, FullHD→~26.5k). LTX = **large-M режим**.

## БАЗОВЫЕ ЗАМЕРЫ (2026-06-03, до оптимизаций)

### bf16 dense — путь LTX (TFLOP/s)
⚠ **ПОПРАВКА (бенч был неверный, как и подозревал пользователь):** первый замер не отключал
grad → `run_linear` падал в matmul-fallback (`gemm_wmma` NN, медленный), а НЕ в реальный
LTX-путь (`backend.linear`→`gemm_bf16` TN, только под **no-grad**). Под NoGradGuard:

| форма | M | cuBLAS | наш best_cu (real) | наш/cuBLAS |
|---|---|---|---|---|
| attn (4096,4096) | 256 | 78.2 | 83.1 | **1.06×** |
| | 512 | 79.6 | 82.8 | 1.04× |
| | 1024 | 84.6 | 82.5 | 0.98× |
| | 2048 | 81.2 | 94.1 | **1.16×** |
| | 4992 | 100.2 | 102.2 | 1.02× |
| | 11960 | 94.3 | 81.3 | 0.86× |
| | 26520 | 102.7 | 84.0 | 0.82× |
| ff_up (16384,4096) | 1024 | 91.1 | 101.5 | 1.11× |
| | 26520 | 93.7 | 82.4 | 0.88× |
| ff_down (4096,16384) | 26520 | 94.2 | 86.5 | 0.92× |

**Вывод: bf16-GEMM ≈ ПАРИТЕТ (0.82-1.16×), на малых/средних M обгоняет cuBLAS. НЕ источник 2× LTX.**
Остаток — только large-M (M≥11960, M%128≠0 → `_part`): 0.82-0.92×. (Старые «8-74» = артефакт grad-on.)

Грязный замер (grad-on, gemm_wmma NN — НЕ путь LTX, для истории): M=256 8.4, M=26520 73.6.

### Квант-GEMM (наш on-the-fly vs qutlass pure), TFLOP/s
| форма / M | cuBLAS bf16 | qutlass nvf4 | наш nvfp4 | qutlass mxf8 | наш mxfp8 |
|---|---|---|---|---|---|
| attn / 256 | 78 | 354 | 135 | 188 | 110 |
| attn / 4992 | 100 | 577 | 156 | 256 | 144 |
| attn / 26520 | 103 | 580 | 132 | 257 | 130 |
| ff_up / 2048 | 100 | 557 | 342 | 271 | 141 |
| ff_up / 26520 | 94 | 482 | 237 | 181 | 147 |
| ff_down / 26520 | 94 | 590 | 112 | 241 | 120 |

Примечание: `SYN_NVFP4_FAST=1 SYN_MXFP8_TILED=1` дали **0 изменений** → наши быстрые ядра
на large-M не активны (работает корректностно-безопасный Broadcast/safe-путь).

---

## TODO / прогресс

### [x] #1 — bf16 dense (best_cu TN) → паритет cuBLAS  *(ЗАКРЫТ 2026-06-04: large-M 0.84× → 1.03×)*
Цель: ≥1.0× cuBLAS на всех M/формах (стретч 1.1×) — **достигнута на large-M** (см. итоги ниже).
**Свежий бэйзлайн (2026-06-04, код после flash_splitq):** attn/11960: наш 84.2 vs cuBLAS 100.8
(0.84×); attn/26520: 83.3 vs 102.3 (0.81×). Старый план «N2/occupancy» оказался НЕРЕЛЕВАНТЕН:
- ncu реального ядра `gemm_bf16_part` @ 11960: 1 блок/SM (132 рег >128, smem 49KB), НО tensor pipe
  **уже 91.9% per-clock**. s2-конфиг (2 стадии, 32KB, `__launch_bounds__(256,2)` → 126 рег → 2 блока/SM,
  occupancy 33%) дал лишь −2.2% циклов. Occupancy — НЕ рычаг, ядро насыщено.
- РЕАЛЬНЫЙ разрыв = **клок (DVFS/энергия) × per-clock**: наш 84.6 sustained = 90% util × 2235 МГц @133W;
  cuBLAS 94.4 = 96.5% × ~2330 МГц @175W. cuBLAS-ядро = `cutlass_80_tensorop_bf16_s16816gemm_256x128_32x3`
  (232 рег, 73.7KB, occupancy 16.7% — НИЖЕ нашей!), L2-throughput 26.5% vs наши 69%.
**Фиксы (все bit-точны vs default + f32-реф ½ULP + row-consistency 0; 16/16 тестов PASS):**
1. g2s-загрузчик: 4 потока/строку × 64B contiguous (было 2×32B-фрагменты на 16 строк/варп) — все конфиги.
2. Тайл **256×128 (b256s3/s4)**: warp-грид 4×2, warp-тайл 64×64, RC=128 рег, ~241 рег/поток — путь cutlass.
   −25% L2-байт/FLOP → меньше энергии → DVFS-клок 2235→**2625 МГц @ 163W** (ниже их 175W!).
3. cp.async БЕЗ `.LTC`-хинта (L2::128B давал лишний LSU-микрооп: SASS LDGSTS.E.BYPASS.LTC).
4. Epilogue: bf162-пары (32-бит сторы) + **`st.global.cs`** (streaming, C не перечитывается → не
   вытесняет горячий B из L2) — главный одиночный скачок +4%.
5. part-гибрид: интерьерные блоки без OOB-предикатов на каждый cp.async (PARTIAL только краевым).
6. Софт-пайплайн: wait+bar в ГОЛОВЕ итерации (барьер-стоилы 9.8%→3.3%), ldmatrix A/B-half1 до 2-й
   MMA-группы, B-half2 (x2-ldmatrix) после. Инстр-микс стал чище cuBLAS (196.6M vs 288.3M total).
**Итог (равный протокол do_bench с L2-flush, idle-гэпы, 2026-06-04):**
| точка | cuBLAS | наш | × |
|---|---|---|---|
| attn/11960 | 101.6 | **105.0** | **1.033** |
| attn/26520 | 103.0 | **106.6** | **1.035** |
| ff_up/11960 | 102.4 | 103.1 | 1.007 |
| ff_down/26520 | 101.3 | 102.4 | 1.011 |
Sustained back-to-back 10s: наш **98.1 vs cuBLAS 94.4 = 1.04×** (163W vs 175W). Остаток per-clock
+1.4% циклов (10.30M vs 10.16M) = качество SASS-шедулинга ptxas — не копать.
**УРОК ПРОТОКОЛА:** triton do_bench флашит L2 (256MB memset) между репами → клоки «дышат» → его числа
≈ burst. Наш старый бенч мерил back-to-back → «0.86×» был наполовину артефактом РАЗНЫХ протоколов.
Добавлен `SYN_BENCH_DOBENCH=1` (флаш+per-iter тайминг) — сравнивать ТОЛЬКО равным протоколом.
**Дефолт-диспетч (data-driven свип):** b256s4 при `m≥4000 || (n≥16384 && m≥2048) || (k≥16384 && m≥1024)`,
иначе старая эвристика (малый M: b256 проигрывает 2× при коротком гриде). env `SYN_BF16_CFG=
s2|s3|s4|s5|s6|b256s3|b256s4` для A/B. ff_up large-M: default был 63-65 TF → 97-100 (+55%).
**Тупики #1 (не повторять):** N2/BN=64 не нужен и требует переделки загрузчика (жёстко BN=128);
occupancy-конфиги (s2) — насыщенному ядру не помогают; pipelining сам по себе циклы не сдвинул
(барьер-стоилы перетекли в wait); L2-raster для part не нужен (L2 hit уже 96.8%, DRAM 217MB/вызов
≈ теоретический минимум).

### [x] #2 — nvfp4 → паритет qutlass nvf4  *(ЗАКРЫТ 2026-06-04: pure-GEMM БЬЁМ qutlass на ВСЕХ формах)*
**ФИНАЛ (равный протокол do_bench+L2-flush, парные свежие замеры, M=26624/26520):**
| форма | qutlass nvf4 | наш rot | × |
|---|---|---|---|
| attn (4096,4096) | 567.7 | **582.0** | **1.025** |
| ff_up (16384,4096) | 482.0 | **588.0** | **1.220** |
| ff_down (4096,16384) | 589.5 | **608.0** | **1.031** |
**Решение = порт k64-конвейера CUTLASS sm120 (`sm120_blockscaled_mma_tma.hpp:814-901`) в
`gn_nvfp4_full_128x256_s3_swz_rot` (ROT=1, fused-producer 256 потоков, REG 254, 0 спиллов):**
1. Регистровый double-buffer фрагментов: ldsm k64-блока kk+1 ДО mma-пачки kk.
2. Ранний release стадии (после последнего ldsm, на пол-тайла раньше) + wait следующей
   стадии ПЕРЕД последней gemm-пачкой — конвейер не дренируется на границе стадий.
3. 🔑 **::cta-фикс TMA**: `shared::cluster` в cp.async.bulk.tensor на sm_120 заставлял ptxas
   вставлять compat-обёртку («extern call», C7506) и МОЛЧА отключать setmaxnreg + замедлял
   UTMALDG. Замена на `shared::cta` (16 мест) дала +5% всем TMA-ядрам И РАЗБЛОКИРОВАЛА
   per-role регистры (USETMAXREG работает: drot384 консьюмеры R237 при «REG:168» в ELF —
   поле REG = CTAPOOL-учёт). ЭТО ЖЕ — корень «стены setmaxnreg» mxfp8_full!
4. Рег-диета ROT-ветки: SF-оффсеты = база+имм (атом (32,4):(16,4) периодичен по mu/nu,
   −12 рег), все smem-адреса от одной базы (−5), SF-пары → LDS.64 (вдвое меньше LDS,
   паритет с qutlass 10.2M warp-inst).
5. smem-staged эпилог (стадии дочитаны → smem свободен): STS + полные STG.128.cs вместо
   64×2Б-сторов (lg_throttle 7.5%→0). Синк bar.sync 7 (консьюмеры; __syncthreads дедлок).
**Per-clock (ncu lock, attn/26624): 3.724M → 3.202M циклов (−14%); qutlass 3.004M (+6.6%
остаток) — но wall у нас ВЫШЕ (256 потоков жгут меньше → DVFS-клок, та же физика что bf16 #1).**
**Диспетч (dispatch.rs): rot = DEFAULT при m≥512 && m%256==0 (паддинг до 256 теперь с
m≥1280); persist-класс ВЫТЕСНЕН (проигрывает rot даже на «своей» ff_up/26624: 372 vs 572).
SYN_NVFP4_NO_ROT=1 — откат.** Корректность: per-row max|Δ|=0 (бит-в-бит, порядок mma
не меняется) на всех формах × M={512..26624}; 20/20 тест-бинарей крейта PASS.
Свип-харнесс: `examples/prof_nvfp4_full_rot.rs` (SHAPE/M/CFG/CHECK/DOBENCH/ITERS).
**Тупики:** drot384 (384 потока, выделенный producer-WG 240/32) = чуть лучше per-clock
(3.24M), но хуже wall (ватты); RDEC=16 ptxas запрещает (мин 24); frot 128×128 хуже дефолта.

### [~] #2-СТАРОЕ (история раундов 1-2)  *(2026-06-04: 112-342 → 307-464 TF)*
**Сделано:**
- 🔥 **Квантизатор активации был 76% времени вызова** (quantize_f16_to_nvfp4: блок=16 потоков
  на 16-группу, 6.8M микроблоков, smem+5×syncthreads). Новое ядро `_fast`: 1 поток = 1 группа,
  2×uint4-лоада, без smem — **×19** (7.66→0.40мс @26520). Бит-в-бит та же арифметика
  (per-row max|Δ|=0 vs slow на всех формах), гейт выравнивания + SYN_NVFP4_QUANT_SLOW.
- ff_down-класс (k>n, вес 32MB): persist-ядро было вдвое медленнее 128×128 (216 vs 440 TF) —
  правило диспетча `weight_fits_l2 || k>n`. ff_up-класс: persist≈128×128 (354/327 vs 338/340, шум).
- Дефолтные числа теперь (back-to-back): attn 363/318/307, ff_up 352/352/327, ff_down 464/446/445
  (M=4992/11960/26520). qutlass pure-GEMM свежий: 437-627; их e2e-эквив (gemm+квант): 419-545.
**Остаток (×1.3-1.9 на large-M):** само Full-ядро. ncu: per-clock ≈ qutlass (375 TF locked у обоих
@attn/26520), но наше на бусте упирается в L2 (66.5% @ locked → сатурация на 2.3+ГГц, wall не
скейлится с клоком). Жирный путь = CUTLASS sm120 block-scaled порт (план (б) из таблицы).
**Раунд 2 (sfb-мультитайл + fused-producer, 2026-06-04):** C_128_256 (тайл qutlass: их грид
(32,104)×384 = 128×256 warp-spec) ПОЧИНЕН и доведён до bit-exact + без спиллов:
1. sfb-мультитайл в шаблоне (зеркально num_w_tiles): per-row 2.9-8.5 → 0.000000;
2. 🔬 **ptxas квантует рег-бюджет по 128 потоков**: блок 288 (8 cons + 1 prod) → бюджет
   65536/384=168 → спиллы STACK 248 (long_scoreboard 54%, 2× медленнее). __maxnreg__(224)
   чинит у nvcc, но NVRTC его игнорирует → ptxas 255 → LAUNCH_OUT_OF_RESOURCES;
3. ✅ Решение: **fused-producer** (PROD_W=0: TMA выпускает tid 0 в консьюмер-цикле, пролог
   S-1 чанков; empty-wait по той же фазовой формуле — дедлока нет) → ровно 256 потоков →
   бюджет 256 → REG 232 / STACK 8. Бит-точен.
Итог A/B: ff_up/26520 +5% (338 vs 321), attn/4992 +5-15% (шумно), остальное ≈/чуть хуже →
**opt-in SYN_NVFP4_BIGTILE=1** (не дефолт). Per-clock 3.72M циклов vs qutlass 3.01M (+24%):
их преимущество в шедулинге k64-конвейера («wait» 31.5% у нас) + wall не скейлится с клоком
у обоих наших вариантов — добивать = полноценный CUTLASS-порт (отдельная сессия).
**Тупики:** (1) C_256_128 (features-256) корректен, но МЕДЛЕННЕЕ 128×128 везде (207-259 vs
272-447), opt-in; (2) .cs-сторы эпилога ≈ нейтральны (секторы заполняются top/bot-парой);
(3) setmaxnreg в теле ядра заставляет ptxas жать регистры — N=0 теперь no-op; (4) persist
на attn нейтрален. Бонус-находка: квант qutlass = CUTLASS-GEMM с Адамаром, 78µs@26520 locked
(наш fast 400µs) — ещё ×5 запаса, если квант снова станет узким.
⚠ Методология: квант-колонки скачут ±20% между процессами (тепло/клок) — A/B только парный
в соседних запусках; ncu locked-clock = стабильная опора.

### [x] #3 — mxfp8 → паритет qutlass mxf8  *(ЗАКРЫТ 2026-06-05: pure-GEMM бьёт qutlass на всех формах)*
**ФИНАЛ (равный do_bench, M=26624/26520, парные свежие замеры 2026-06-05):**
| форма | qutlass mxf8 | старое ядро | **rot-drot** | × qutlass |
|---|---|---|---|---|
| attn | 253.2 | 249.6 | **273.5** (GR=32) | **1.080** |
| ff_up | 179.5 | 194.8 | **249.4** (GR=8) | **1.389** |
| ff_down | 229.8 | 225.3 | **240.0** | **1.044** |
**Решение = тот же рецепт, что nvfp4 (#2): `gn_mxfp8_drot_128x128_s2`** — TMA(::cta)+mbarrier,
ротация по ПАРАМ k32-блоков (x4-ldmatrix B кроет 2 суб-блока → структура ≡ nvfp4 KCH=2),
выделенный producer-warpgroup (384 потока, setmaxnreg 240/24), staged-эпилог, S=2 стадии
(SF-окна TMA 16Б×4-стадии — inner-dim минимум TMA; данные+SF+барьеры = 73.8KB).
🔑 **L2-растр обязателен** (1D-грид → окно волны GR N-тайлов × M-полоса): ff_up (веса 64MB>L2)
GR=8: 180.7→249.4 (+38%); attn (8MB≤L2) GR=32. Дефолт shape-aware (n·k≤24MB→32, иначе 8),
SYN_MXFP8_RASTER override. GR=1 (M-полосы) катастрофа: 153-176 TF.
**Wiring: `mxfp8_linear_tiled` → drot по умолчанию** (k%512==0; SYN_MXFP8_NO_ROT=1 — откат).
Bit-exact (per-row max|Δ|=0 vs gn_mxfp8_128x128 — k-порядок mma сохранён) на всех формах;
314/314 тестов PASS. Per-clock @ attn/26624: старое 8.37M (occupancy 8.3%!, 1 варп/шедулер)
→ drot 6.81M циклов; qutlass 5.99M (+13.7% остаток) — wall компенсирует (клок/ватты).
**Гочи:** RDEC=32 (пул 65536 ровно) ВИСНЕТ на mxfp8-drot (nvfp4-drot с 32 живёт) → RDEC=24
(продьюсер чуть спиллит, 532K local_ld — терпимо); TMA inner-box <16Б → INVALID_VALUE.
**Уроки трекера ДО порта были артефактом протокола:** «162-186 vs 241-257» сравнивал наш E2E
(квант+паддинг) против их pure; pure-vs-pure старое ядро уже было 0.98-1.09×.
Харнесс: `examples/prof_mxfp8_rot.rs` (SHAPE/M/CFG=base|rot|drot/CHECK/DOBENCH).

### [x] #4 — Reclaim простаивающих быстрых квант-ядер  *(ВЫВОД: reclaim'ить нечего)*
Аудит dispatch (`gemm/dispatch.rs`, `cuda_backend.rs`):
- `SYN_NVFP4_FAST`/`SYN_MXFP8_TILED` в коде **уже нет** (стейл из памяти). Реальный гейт nvfp4 =
  `SYN_NVFP4_NO_FULL` (Full **включён по умолчанию**). На формах LTX (m≥128, k%128==0) `pick_nvfp4`
  возвращает **Full** → 112-342 это и есть наш потолок Full-ядра, не Broadcast.
- mxfp8: `gemm_mxfp8_full` (205TF) **УДАЛЁН** при чистке. Прод mxfp8 prefill = `mxfp8_linear_tiled`
  (базовый `gemm_mxfp8.cu`, порт gau-nernst, cp.async) → 120-158. Память про 282TF — это pure-GEMM
  на Qwen-формах без act-quant; наш замер с act-quant на LTX-формах ниже → разрыв = квантизатор (#5)
  и/или выбор конфига под LTX-формы.
**Итог:** бесплатных «флагом» ядер нет. Рычаги = #1-#3 (качество ядер) + #5 (изоляция квантизатора).

### [ ] #5 — Активационный квант: изолировать pure-GEMM, догнать fusedQuantize
Перемерить `linear_quant_prequant` (чистый GEMM) + наш квантизатор vs fused qutlass.
**Прогресс:** _не начато_

### [ ] #6 — E2E-верификация: GEMM-паритет реально закрыл 2× LTX  *(после #1-#3)*
Перемерить e2e LTX vs python, real-clock A/B по фазам, найти остаточные не-GEMM bottleneck'и.
**Прогресс:** _не начато_

---

## RECON ТТХ (2026-06-03, roofline — считаем, не гадаем)
RTX 5090 Laptop: **82 SM, 3.09 ГГц, D2D эфф. 779 GB/s** (пик ~896), **пик bf16 (cuBLAS) 102 TF**.
PCIe H2D: **pageable 3.6 GB/s vs pinned 45 GB/s (12×!)**.
- bf16-GEMM compute-bound, потолок 102 TF. attn(4096²)=100%, **ff(N/K=16384)=83-92% SM-util** —
  ncu: Compute 92.5%, DRAM 7.9%, L2-hit 95% → ff почти выжат; разрыв до cuBLAS = плотность
  tensor-инструкций inner-loop (самый дорогой/низкоокупаемый win). part(4080)≈swz(4096) (~2%).
- **Узкое место DiT — НЕ GEMM, а декомпозированные norm/cast** (per-kernel ncu блока): `reduce_f32`
  1.96мс + `cast_*` 1.1-1.6мс — rms-norm идёт cast→sqr→reduce→div→cast→contiguous (~6 ядер,
  23× медленнее BW), ×3/блок ≈ 15мс/блок. adaln-linear (N=36864) 21.6мс раз/шаг (минор).

## Прогресс перфа LTX (e2e, грид 8×17×30=4080 ток, 8 шагов)
- Базлайн: **74.0с**.
- Offload pageable→**pinned** (45 GB/s) + **префетч** (bg-поток, loader-stream): **64.6с (1.145×)**,
  качество-нейтрально. Оверлап частичен (bg делает mmap→pinned memcpy + H2D).
- **Fused rms-norm** (вместо decomposed, F32-faithful, 1 ядро vs 6): **53.9с (−9с от 62.9)**. Кумулятивно **74→53.9с = 1.37×**.
  Гейт `SYN_LTX_NO_FUSED_RMS` / `_GAIN`. КАЧЕСТВО: ядро `rms_norm_fused` изолированно vs decomposed = 0.25%
  (=bf16-округление выхода, корректно). Forward на OOD-входах (constant/randn вне траектории) врал (1.9-50%
  хаос). Реальный гейт = пиксельное сравнение видео (seed детерминирован) — в процессе. НЕ принимать пока
  не подтверждён real-trajectory дрейф ≈ bf16-шум.
  ИТОГ fused-rms: **ядро корректно (0.25%=bf16-шум)**, но **видео ≠ на 131%** (тот же seed) — distilled-LTX
  хаотичен, bf16-разница меняет траекторию (класс FLUX). → **OPT-IN, по умолчанию ВЫКЛ** (`SYN_LTX_FUSED_RMS=1`).
  Качество-нейтральный подтверждённый результат пока = **offload 74→64.6с (1.145×)**. fused-rms (1.37×) — после
  визуальной/python-валидации «другой сэмпл всё ещё хорош».
  УРОК (recon): нельзя менять ЛЮБОЕ ядро на горячем пути distilled-DiT не bit-identical — хаос усиливает
  bf16-разницу до другого видео. Оптимизации обязаны быть bit-identical ЛИБО валидироваться визуально/vs-python.

## ВИЗУАЛЬНАЯ ВАЛИДАЦИЯ (ключевой сдвиг методологии)
fused-rms кадр vs decomposed (тот же seed, prompt «кофе»): **ВИЗУАЛЬНО ИДЕНТИЧНЫ** (та же чашка/пар/
закат/композиция/качество), хотя max|Δ|=131%. → max-abs метрика ОБМАНЫВАЕТ (единичные пиксели); хаос
distilled-LTX **доброкачественный** — bf16-уровневые изменения ядер дают визуально-эквивалентный сэмпл.
**ГЕЙТ КАЧЕСТВА = визуальное сравнение кадра (`SYN_LTX_SAVE_FRAMES`), НЕ bit-identity и НЕ max-abs.**
→ fused-rms ВКЛ по умолчанию (1.37×). Открыты flash/norm-опты под визуальную валидацию.
Кумулятив: **74→53.9с = 1.37×** (offload pinned+prefetch bit-identical + fused-rms визуально-эквив.).

## RECON attn1 (per-call, T=4080): flash 9.86мс (№1, ~27% эфф = latency-bound softmax) /
rms_decomp 4.16мс (×3-5/блок) / transpose 0.7мс (мелочь). Следующий рычаг = flash softmax-параллелизм.

## Тупики (recon-проверено, не тратить время)
- ~~bf16 large-M тюнинг: GEMM на пике 102 TF (=cuBLAS). Нечего.~~ **ОПРОВЕРГНУТО 2026-06-04:**
  102 TF — не пик железа, а пик cuBLAS; b256-тайл + энергоэффективность дали 105-107 TF (см. #1).
- mxfp8-resident DiT (убрать offload): denoise 44→41.6с (мизер — префетч УЖЕ спрятал стрим) + OOM на VAE
  (22GB резидент + VAE >24GB). Offload+prefetch лучше.
- fused-rms «потеря качества»: НЕТ — визуально идентично (max-abs метрика врала).

## ИТОГ сессии: e2e 74.0с → 53.9с = **1.37×**, качество подтверждено
- offload pageable→pinned + префетч: 74→64.6 (**bit-identical**, проверено CHECK_VID rel=0).
- fused rms-norm (1 ядро vs 6): 64.6→53.9 (**визуально идентичный кадр**, гейт = SAVE_FRAMES).
- Остаток (compute-bound, стрим спрятан): denoise 44с (attn1 50% / attn2 25% / ff 25%, GEMM на пике) + VAE 11.3с.
- Длинный хвост дальше: flash softmax-параллелизм (warp0-only→все варпы, ~2с, визуально-валидировать),
  VAE 11.3с (21%, conv3d), attn-проекции (на пике bf16). Структурные большие выигрыши исчерпаны.
- TODO для подтверждения «паритета»: замерить python-эталон LTX на той же сетке (~Temp/LTX-2 .venv).

## Попытки, которые НЕ сработали (откачены через git)
- flash softmax распределить по 4 варпам (warp0-only → все): **56.1с (регресс +2с)** — intra-warp
  divergence (4/32 lane активны в каждом варпе) хуже warp0 (16/32). Откачено. Правильный фикс = 8-потоков-
  на-строку с shuffle-редукцией (рискованный rewrite ×2 копии, ~2с приза) — не делал.
- mxfp8-resident: кадр когерентен (качество OK), но perf-тупик (OOM + мизер).

## Журнал
- 2026-06-04 (CUTLASS-порт, отдельная сессия): **#2 ЗАКРЫТ — nvfp4 pure-GEMM бьёт qutlass
  на всех формах (1.025/1.220/1.031×)**. Ключи: ротация k64-конвейера по CUTLASS-схеме
  (double-buffer фрагментов + ранний release + wait перед последней gemm-пачкой),
  🔑 ::cta-фикс TMA (::cluster глушил setmaxnreg через ptxas C7506 «extern call» — корень
  «стены setmaxnreg sm_120» из mxfp8_full!), рег-диета (SF база+имм, LDS.64-пары),
  smem-staged эпилог (lg_throttle 7.5→0). Per-clock 3.724M→3.202M (qutlass 3.004M, но wall
  наш выше — DVFS/ватты). rot = default диспетча (m≥512, m%256==0, паддинг 256 с m≥1280),
  persist вытеснен. Bit-exact per-row 0 везде, 20/20 тестов PASS. Урок методики: ptxas
  «квантование рег-бюджета по 128 потоков» из прошлых сессий было СЛЕДСТВИЕМ ::cluster-TMA,
  а не свойством sm_120.
- 2026-06-04 (продолжение): **строки #2-#4 пользовательской таблицы (bf16 M=26520) верифицированы**
  чистым попарным протоколом (do_bench оба, idle-гэпы, медианы 2 раундов; свежие якоря ВЫШЕ табличных:
  102.4/102.4/101.5 vs 102.7/93.7/94.2): attn 106.2 (**1.037×** ✓), ff_down 104.2 (**1.027×** ✓),
  ff_up 102.7 (**1.003× = паритет**, был 0.92×). Фикс ff_up/ff_down: **растр (N-чанкинг grid.z) включён
  в part-гибрид** (B=128MB не лезет в L2 64MB; x-major перечитывал B каждый y-страйп). Stride 2048≈4096,
  8192 хуже (чанк=весь L2); env SYN_BF16_SWZ. ff_up остаток: per-clock 88.67M vs cuBLAS 87.16M циклов
  (+1.7%) при SM-util 97.31% (насыщение), DRAM-байты идентичны (2.74GB), L2 hit 95.2 vs 93.7 —
  остаток = ручной SASS-шедулинг NVIDIA, компенсируется нашим клоком до 1.00×. Дожим ff_up >1.01×
  (smem-staged epilogue STG.128 / mbarrier producer-consumer) отложен: ROI ниже строк #5-#8 (разрывы 2-5×).
- 2026-06-04: **#1 ЗАКРЫТ** — bf16 large-M 0.84×→1.03× (attn/11960: 105.0 vs cuBLAS 101.6; 26520:
  106.6 vs 103.0; sustained 1.04×). Ключ: НЕ occupancy (ядро было насыщено 91.9%), а энергия/клок —
  тайл 256×128 (−25% L2/FLOP) + contiguous-загрузчик + st.global.cs epilogue → DVFS 2235→2625 МГц.
  Урок: на ноутбучном GPU ватты = перф; сравнивать строго РАВНЫМ протоколом (do_bench флашит L2 —
  «дыхание» клоков; добавлен SYN_BENCH_DOBENCH=1). Дефолт b256s4 на large-M, корректность bit-exact,
  16/16 тестов PASS. ff_up large-M +55% (63→100 TF).
- 2026-06-03: базовые замеры сняты (последовательно, под лимитом памяти). Заведены #1-#6.
- 2026-06-03: #4 закрыт (reclaim'ить нечего). #1 диагностика по ncu вышла на ЛОЖНОЕ ядро
  (`gemm_wmma` NN), т.к. бенч был grad-on.
- 2026-06-03 **БОЛЬШАЯ ПОПРАВКА**: добавил NoGradGuard в бенч → реальный путь LTX =
  `gemm_bf16` TN. **bf16 = 0.82-1.16× cuBLAS (паритет, малый/средний M обгоняет)**, а не 0.11-0.72×.
  N2(BN=64)-эксперимент откатан (решал несуществующую проблему; SWZ-путь был багнут OOB, gated off).
  **СЛЕДСТВИЕ — ПЕРЕПРИОРИТИЗАЦИЯ:** bf16-GEMM не источник 2× LTX → #1 почти закрыт (residual large-M
  0.82-0.92×, низкий приоритет). Главный рычаг теперь **#6 (e2e-профиль)**: найти РЕАЛЬНЫЙ источник 2×
  (кандидаты: layer-offload стриминг mmap→VRAM, flash-attention, не-GEMM ops, rope/norm). #2/#3 (quant
  vs qutlass) — только если решим квантовать DiT (сейчас bf16 ради качества) или для общей либы ядер.
  СЛЕД. ШАГ: профиль LTX DiT forward (SYN_LTX_PROF) vs python по фазам.
